create schema goride;

set search_path to goride;

create table auto(idno integer primary key unique not null,vehicletype text not null, rangekm integer not null, acprice integer, nonacprice integer); 
create table micro(idno integer primary key unique not null,vehicletype text not null, rangekm integer not null, acprice integer, nonacprice integer);
create table xl(idno integer primary key unique not null,vehicletype text not null, rangekm integer not null, acprice integer, nonacprice integer);
create table vehicle(idno integer not null, vehicletype text not null);

alter table vehicle add foreign key (idno) references auto(idno); 
alter table vehicle add foreign key (idno) references micro(idno); 
alter table vehicle add foreign key (idno) references xl(idno); 

insert into auto values ('1', 'auto', '0', '15', '10');
insert into auto values ('2', 'auto', '15', '30', '8');
insert into auto values ('3', 'auto', '30', '1500', '15');

insert into micro values ('1', 'micro', '0', '15', '12', '14');
insert into micro values ('2', 'micro', '15', '1500', '10', '12');

insert into xl values ('1', 'xl', '0', '15', '14', '15');
insert into xl values ('2', 'xl', '15', '1500', '14', '15');
